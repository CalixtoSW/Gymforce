# Handoff HO-0003 — BL-003

## Identificação

| Campo | Valor |
|-------|-------|
| **Handoff ID** | HO-0003 |
| **Data** | 2026-04-11 11:25 |
| **De (origem)** | Agente Dev Mobile (Codex) |
| **Para (destino)** | Arquiteto Chefe |
| **Ticket/Backlog** | BL-003 |
| **Módulo** | M0 |
| **Branch** | feat/M0-mobile-scaffold |

## O que foi feito

Scaffold mobile com Expo Router em TypeScript, incluindo grupos `(auth)` e `(tabs)`, 5 tabs com navegação funcional, design tokens centralizados, client HTTP base com Axios, estrutura de diretórios para evolução dos próximos BLs e placeholders compiláveis.

### Arquivos criados/modificados

```text
+ mobile/app/_layout.tsx
+ mobile/app/index.tsx
+ mobile/app/(auth)/_layout.tsx
+ mobile/app/(auth)/login.tsx
+ mobile/app/(auth)/register.tsx
+ mobile/app/(tabs)/_layout.tsx
+ mobile/app/(tabs)/home.tsx
+ mobile/app/(tabs)/workouts.tsx
+ mobile/app/(tabs)/rewards.tsx
+ mobile/app/(tabs)/leaderboard.tsx
+ mobile/app/(tabs)/profile.tsx
+ mobile/components/ui/Button.tsx
+ mobile/components/features/.gitkeep
+ mobile/components/layout/.gitkeep
+ mobile/hooks/useColorScheme.ts
+ mobile/services/api.ts
+ mobile/stores/.gitkeep
+ mobile/types/index.ts
+ mobile/utils/.gitkeep
+ mobile/constants/theme.ts
+ mobile/assets/fonts/.gitkeep
+ mobile/assets/images/.gitkeep
+ mobile/app.json
+ mobile/tsconfig.json
+ mobile/package.json
+ mobile/package-lock.json
+ mobile/babel.config.js
+ mobile/.eslintrc.js
+ mobile/eslint.config.js
+ docs/handoff/HO-0003.md
```

### Decisões tomadas durante a execução

1. Base de roteamento com Expo Router puro (sem uso direto de react-navigation) conforme diretriz.
2. Root layout com `QueryClientProvider` + carregamento de fonte e splash control.
3. Placeholders de telas com estilo consistente via tokens em `constants/theme.ts`.

## O que NÃO foi feito (e por quê)

| Item Pendente | Motivo | Prioridade |
|--------------|--------|-----------|
| Lógica de autenticação real | Fora de escopo (BL-014) | Alta |
| Componentes de feature (fichas, pontos, etc.) | Fora de escopo BL-003 | Alta |

## Estado atual

### O que funciona ✅
- App compila com TypeScript.
- Tabs e rotas de auth estão navegáveis e estáveis.
- `constants/theme.ts` exporta tokens tipados.
- `services/api.ts` exporta client Axios com baseURL por ambiente.

### O que está quebrado ❌
- Nenhum item conhecido no escopo BL-003.

### O que precisa de atenção ⚠️
- `.eslintrc.js` foi mantido por diretriz de estrutura, mas o lint roda via `eslint.config.js` (ESLint v9).

## Contexto técnico para o próximo agente

### Dependências instaladas nesta sessão
```bash
npx expo install expo-router expo-font expo-splash-screen expo-status-bar expo-secure-store expo-constants
npm install zustand @tanstack/react-query zod react-hook-form @hookform/resolvers axios
npm install -D eslint eslint-config-expo
```

### Como rodar/testar o que foi feito
```bash
cd mobile
npm install
npm run lint
npx tsc --noEmit
npx expo start
```

## Próximos passos sugeridos

1. [ ] Abrir PR `feat/M0-mobile-scaffold` -> `develop`.
2. [ ] Iniciar BL-014 (auth) reutilizando grupo `(auth)` e `services/api.ts`.
3. [ ] Definir componentes de design system sobre `components/ui/Button.tsx`.
