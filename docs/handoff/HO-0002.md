# Handoff HO-0002 — BL-002

## Identificação

| Campo | Valor |
|-------|-------|
| **Handoff ID** | HO-0002 |
| **Data** | 2026-04-11 11:10 |
| **De (origem)** | Agente Dev Backend (Codex) |
| **Para (destino)** | Arquiteto Chefe |
| **Ticket/Backlog** | BL-002 |
| **Módulo** | M0 |
| **Branch** | feat/M0-backend-scaffold |

## O que foi feito

Scaffold completo do backend FastAPI com arquitetura base para evolução dos próximos BLs: app factory, health check `/api/v1/health`, configuração via `BaseSettings`, engine/sessão assíncrona de SQLAlchemy, base de models com mixins, setup Alembic assíncrono, suíte inicial de testes, Dockerfile, `.env.example`, `pyproject.toml` e versão Python.

### Arquivos criados/modificados

```text
+ backend/.python-version
+ backend/.env.example
+ backend/Dockerfile
+ backend/pyproject.toml
+ backend/alembic.ini
+ backend/alembic/env.py
+ backend/alembic/script.py.mako
+ backend/alembic/versions/.gitkeep
+ backend/app/__init__.py
+ backend/app/main.py
+ backend/app/api/__init__.py
+ backend/app/api/deps.py
+ backend/app/api/v1/__init__.py
+ backend/app/api/v1/health.py
+ backend/app/core/__init__.py
+ backend/app/core/config.py
+ backend/app/core/database.py
+ backend/app/core/security.py
+ backend/app/models/__init__.py
+ backend/app/models/base.py
+ backend/app/schemas/__init__.py
+ backend/app/schemas/health.py
+ backend/app/services/__init__.py
+ backend/app/repositories/__init__.py
+ backend/tests/__init__.py
+ backend/tests/conftest.py
+ backend/tests/test_health.py
+ docs/handoff/HO-0002.md
```

### Decisões tomadas durante a execução

1. `CORS_ORIGINS` recebe lista padrão e parse de string CSV para compatibilidade com `.env`.
2. Health check mantém `status="ok"` e reporta `database="ok|error"` sem quebrar o endpoint em falha de conexão.
3. `alembic/versions` ficou vazio com `.gitkeep`; migration de validação foi removida após execução bem-sucedida do autogenerate.

## O que NÃO foi feito (e por quê)

| Item Pendente | Motivo | Prioridade |
|--------------|--------|-----------|
| Implementação de autenticação real | Fora do escopo (BL-013) | Alta |
| Models de domínio (users/checkins/etc.) | Fora do escopo de BL-002 | Alta |

## Estado atual

### O que funciona ✅
- App FastAPI sobe com docs em `/api/docs`.
- Endpoint `GET /api/v1/health` retorna 200 com payload esperado.
- Lint (`ruff`) e teste inicial (`pytest`) passam.
- Alembic assíncrono configurado e `revision --autogenerate` executa sem erro.

### O que está quebrado ❌
- Nenhum item conhecido dentro do escopo BL-002.

### O que precisa de atenção ⚠️
- `docker-compose.yml` emite warning de `version` obsoleta (fora do escopo deste BL).

## Contexto técnico para o próximo agente

### Dependências instaladas nesta sessão
```bash
cd backend
python3 -m venv .venv
. .venv/bin/activate
pip install -e ".[dev]"
```

### Variáveis de ambiente novas
```bash
APP_ENV=development
APP_DEBUG=true
DATABASE_URL=postgresql+asyncpg://gym:gym_dev_2026@localhost:5432/gymforce
REDIS_URL=redis://localhost:6379/0
JWT_SECRET_KEY=CHANGE-ME-IN-PRODUCTION
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
CORS_ORIGINS=http://localhost:8081,http://localhost:3000
```

### Como rodar/testar o que foi feito
```bash
cd backend
python3 -m venv .venv
. .venv/bin/activate
pip install -e ".[dev]"
uvicorn app.main:app --reload
# Health
curl http://localhost:8000/api/v1/health
# Docs
# http://localhost:8000/api/docs
# Testes
pytest -q
# Lint
ruff check .
```

## Próximos passos sugeridos

1. [ ] Abrir PR `feat/M0-backend-scaffold` -> `develop`.
2. [ ] Iniciar BL-013 (auth) reutilizando `core/security.py` e `api/deps.py`.
3. [ ] Definir primeiro modelo de domínio + migration inicial real no próximo BL.
